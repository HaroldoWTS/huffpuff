#include "huffpuff-dom.h"

int huffpuff_dom_from_file(huffpuff_dom_t * dom, FILE * in){
	if (!dom || !in)
		return 0;
	fread(dom->magic, 9, sizeof()	
}

int huffpuff_dom_to_file(huffpuff_dom_t * dom, FILE * out){

}


